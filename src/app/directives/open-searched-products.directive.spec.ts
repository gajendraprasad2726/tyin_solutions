import { OpenSearchedProductsDirective } from './open-searched-products.directive';

describe('OpenSearchedProductsDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenSearchedProductsDirective();
    expect(directive).toBeTruthy();
  });
});
